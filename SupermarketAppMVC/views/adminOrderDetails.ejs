<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Order #<%= order ? (order.id || order.orderId) : '' %> | Details</title>
  
  <!-- Fonts & Icons -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

  <style>
    :root {
      --primary-color: #4f46e5;
      --text-main: #111827;
      --text-muted: #6b7280;
      --bg-body: #f3f4f6;
      --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg-body);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .main-content {
      flex: 1;
      padding: 2rem 0;
    }

    .container-custom {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    /* Cards */
    .card-section {
      background: white;
      border-radius: 12px;
      border: 1px solid #e5e7eb;
      box-shadow: var(--card-shadow);
      padding: 1.5rem;
      /* height: 100%;  <-- REMOVED: Caused layout bugs in right column */
    }

    .section-title {
      font-size: 0.95rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-muted);
      margin-bottom: 1.25rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid #f3f4f6;
    }

    /* Customer Info List */
    .info-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .info-list li {
      display: flex;
      margin-bottom: 1rem;
      gap: 1rem;
    }
    .info-icon {
      color: var(--text-muted);
      font-size: 1.1rem;
      width: 24px;
    }
    .info-content .label {
      display: block;
      font-size: 0.75rem;
      color: var(--text-muted);
      margin-bottom: 2px;
    }
    .info-content .value {
      font-weight: 500;
      font-size: 0.95rem;
      color: var(--text-main);
    }

    /* Table */
    .table-custom th {
      background-color: #f9fafb;
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-muted);
      padding: 1rem;
      border-bottom: 1px solid #e5e7eb;
    }
    .table-custom td {
      padding: 1rem;
      vertical-align: middle;
      border-bottom: 1px solid #f3f4f6;
    }
    .table-custom tr:last-child td {
      border-bottom: none;
    }

    /* Status Badge */
    .badge-status {
      padding: 0.5rem 1rem;
      border-radius: 9999px;
      font-size: 0.875rem;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }
    .badge-status::before {
      content: '';
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background-color: currentColor;
    }
    .status-pending { background: #fff7ed; color: #c2410c; }
    .status-processing { background: #eff6ff; color: #1d4ed8; }
    .status-shipped { background: #f0fdf4; color: #15803d; }
    .status-delivered { background: #f0fdf4; color: #15803d; }
    .status-cancelled { background: #fef2f2; color: #b91c1c; }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <main class="main-content">
    <div class="container-custom">
      
      <!-- Page Header -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <div class="d-flex align-items-center gap-3">
          <a href="/admin/orders" class="btn btn-light border bg-white text-muted">
            <i class="bi bi-arrow-left"></i>
          </a>
          <div>
            <div class="d-flex align-items-center gap-3">
              <h2 class="h3 fw-bold mb-0">Order #<%= order.id || order.orderId %></h2>
              <% 
                const s = (order.status || 'Pending').trim(); 
                let badgeClass = 'bg-light text-dark';
                if(s === 'Pending') badgeClass = 'status-pending';
                if(s === 'Processing') badgeClass = 'status-processing';
                if(s === 'Shipped') badgeClass = 'status-shipped';
                if(s === 'Delivered') badgeClass = 'status-delivered';
                if(s === 'Cancelled') badgeClass = 'status-cancelled';
              %>
              <span class="badge-status <%= badgeClass %>"><%= s %></span>
            </div>
            <span class="text-muted small">
              Placed on <%= order.createdAt ? new Date(order.createdAt).toLocaleString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) : '-' %>
            </span>
          </div>
        </div>
        <div class="d-flex gap-2">
            <!-- Example Actions -->
            <form action="/admin/orders/<%= order.id || order.orderId %>/status" method="POST" style="display:inline;">
                 <!-- Quick Action for next logical step could go here, for now just a wrapper -->
            </form>
        </div>
      </div>

      <div class="row g-4">
        
        <!-- Left Column: Order Items -->
        <div class="col-lg-8">
          <div class="card-section p-0 overflow-hidden">
            <div class="p-4 border-bottom bg-light bg-opacity-25">
               <h5 class="mb-0 fw-bold">Order Items</h5>
            </div>
            <div class="table-responsive">
              <table class="table table-custom mb-0">
                <thead>
                  <tr>
                    <th style="width: 50%;">Product</th>
                    <th class="text-center">Price</th>
                    <th class="text-center">Quantity</th>
                    <th class="text-end">Total</th>
                  </tr>
                </thead>
                <tbody>
                  <% let sum = 0; %>
                  <% if (items && items.length > 0) { %>
                    <% items.forEach(function(item) { 
                       const price = Number(item.price || 0);
                       const qty = item.quantity || item.cartQty || 0;
                       const total = price * qty;
                       sum += total;
                    %>
                    <tr>
                      <td>
                        <div class="d-flex align-items-center gap-3">
                          <% if(item.image) { %>
                            <img src="/images/<%= item.image %>" alt="Product" class="rounded border" style="width: 48px; height: 48px; object-fit: cover;">
                          <% } else { %>
                            <div class="bg-light rounded border d-flex align-items-center justify-content-center text-muted" style="width: 48px; height: 48px;">
                                <i class="bi bi-box"></i>
                            </div>
                          <% } %>
                          <div>
                            <div class="fw-medium text-dark"><%= item.productName || 'Product #' + item.product_id %></div>
                            <small class="text-muted">ID: <%= item.product_id %></small>
                          </div>
                        </div>
                      </td>
                      <td class="text-center">$<%= price.toFixed(2) %></td>
                      <td class="text-center"><%= qty %></td>
                      <td class="text-end fw-medium">$<%= total.toFixed(2) %></td>
                    </tr>
                    <% }); %>
                  <% } else { %>
                    <tr><td colspan="4" class="text-center py-4 text-muted">No items found.</td></tr>
                  <% } %>
                </tbody>
              </table>
            </div>
            
            <!-- Summary Footer -->
            <div class="p-4 bg-light bg-opacity-10 border-top">
                <div class="row justify-content-end">
                    <div class="col-md-5">
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-muted">Subtotal</span>
                            <span class="fw-medium">$<%= sum.toFixed(2) %></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-muted">Shipping</span>
                            <span class="text-success">Free</span>
                        </div>
                        <div class="d-flex justify-content-between pt-3 border-top mt-3">
                            <span class="fw-bold fs-5">Total</span>
                            <span class="fw-bold fs-5 text-primary">$<%= (order.totalAmount || order.total || sum).toFixed(2) %></span>
                        </div>
                    </div>
                </div>
            </div>
          </div>
        </div>

        <!-- Right Column: Customer Details -->
        <div class="col-lg-4">
          
          <!-- Customer Card -->
          <div class="card-section mb-4">
            <div class="section-title">Customer Details</div>
            <div class="d-flex align-items-center gap-3 mb-4">
                <div class="bg-primary bg-opacity-10 text-primary rounded-circle d-flex align-items-center justify-content-center fw-bold fs-4" style="width: 56px; height: 56px;">
                    <%= (order.username || 'U').charAt(0).toUpperCase() %>
                </div>
                <div>
                    <h5 class="mb-0 fw-bold"><%= order.username || 'Unknown User' %></h5>
                    <div class="text-muted small">Customer ID: #<%= order.user_id %></div>
                </div>
            </div>
            
            <ul class="info-list">
                <li>
                    <i class="bi bi-envelope info-icon"></i>
                    <div class="info-content">
                        <span class="label">Email Address</span>
                        <div class="value"><%= order.email || 'N/A' %></div>
                    </div>
                </li>
                <li>
                    <i class="bi bi-telephone info-icon"></i>
                    <div class="info-content">
                        <span class="label">Phone Number</span>
                        <div class="value"><%= order.contact || 'N/A' %></div>
                    </div>
                </li>
            </ul>
          </div>

          <!-- Shipping/Billing Card -->
          <div class="card-section">
            <div class="section-title">Shipping Address</div>
            <div class="d-flex gap-3">
                <i class="bi bi-geo-alt info-icon mt-1"></i>
                <div>
                    <% if(order.address) { %>
                        <div class="fw-medium text-dark lh-base">
                            <%= order.address %>
                        </div>
                        <!-- Optional: Mock city/country if you don't have distinct fields -->
                        <div class="text-muted small mt-1">Singapore</div> 
                    <% } else { %>
                        <span class="text-muted fst-italic">No address provided</span>
                    <% } %>
                </div>
            </div>
            
            <div class="mt-4 pt-3 border-top">
                <div class="section-title mb-2" style="border:none;">Payment Info</div>
                <div class="d-flex align-items-center gap-2 text-muted">
                    <i class="bi bi-credit-card"></i>
                    <span>Paid via Credit Card</span>
                </div>
            </div>
          </div>

        </div>
      </div>

    </div>
  </main>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>